name: GitHub Pages

on:
  workflow_run:
    workflows: ["Update Commits"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup Pages
        run: |
          # Add your steps to deploy GitHub Pages
